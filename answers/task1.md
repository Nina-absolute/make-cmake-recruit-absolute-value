# Task 1：从 C 源码到可执行文件

>请用自己的语言简要说明下面几个阶段分别做了什么：
>
>1. 预处理
>2. 编译
>3. 汇编
>4. 链接
>
>建议控制在 300～500 字，不需要展开复杂细节。
// 复杂细节详见文件`note.md`中`## #1 编译流程笔记（详细版）`
// 详细版笔记内含全流程详细分析:thumbsup:

## 1. 预处理（`.c`->`.i`）
// `gcc -E hello.c -o hello.i`

预处理`.c`源文件时：
处理 `#include`、`#define` 和条件编译，把头文件内容和宏展开，得到 `.i` 文件。

**预处理 = 处理掉`#include`等等，把源文件 -> 纯 C 代码文件。**

## 2. 编译（`.i`->`.s`）
// `gcc -S hello.i -o hello.s`

编译预处理文件时：
把预处理后的 C 代码翻译成汇编代码，同时检查语法和类型问题，得到 `.s` 文件。

**编译 = 经过检查和优化，把纯 C 代码 -> 接近机器语言的汇编文件。**

## 3. 汇编（`.s` -> `.o`）
// `gcc -c hello.s -o hello.o`

汇编文件变为机器码时：
把汇编代码翻译成机器码，生成 `.o` 目标文件。`.o` 里包含节区、符号表和重定位表，但还不能直接运行。

**汇编 = 把汇编代码转换成机器码，生成目标文件。**

## 4. 链接（）
// `gcc hello.o -o hello`

解析 `printf` 等外部符号，合并节区，根据重定位表修正地址引用，加入启动代码并设置入口点，最后生成可执行文件 `hello`。

**链接 = 把多个目标文件以及需要的库组合起来，最终生成可执行程序。**

